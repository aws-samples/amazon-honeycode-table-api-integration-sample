{
  "name": "exportcontacthistory",
  "version": "1.0.0",
  "description": "This lambda uses the Honeycode GetScreenData to read pending transactions, execute them, save transactions to an s3 bucket and finally use InvokeScreenAutomation API to clean up completed transactions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kskandha@amazon.com",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.798.0",
    "csv-stringify": "^5.5.3"
  }
}
